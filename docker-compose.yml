version: '3.9'

services:
  web:
    build: .
    command: gunicorn --chdir backend SoundWave.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
      - media_volume:/app/backend/media
      - static_volume:/app/backend/staticfiles
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DJANGO_ALLOWED_HOSTS=*
      - DJANGO_DB_ENGINE=sqlite

volumes:
  media_volume:
  static_volume:
